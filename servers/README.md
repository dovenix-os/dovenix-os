# servers

Userspace system servers: `devmgr` (driver supervisor), `vfsd`, `netd`, `posixd`, …

- Servers depend only on `libs/` and speak protocols to everything else.
- License scope: `Apache-2.0 OR MIT`.
- Status: not started (devmgr lands with milestone M2).
